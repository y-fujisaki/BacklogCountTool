# BacklogCountTool
## 1. ライブラリインストール
* スクリプトのあるフォルダで下記を実行

```
pip install requests -t .
pip install datetime -t .
pip install python-dateutil -t .
pip install slackweb -t .
```

## 2. 設定値の変更


## 3. zipで圧縮

```
zip -r upload.zip *
```

## 4. 